package constants

const (
	BasePath        = "/api"
	V1BasePath      = "/api/v1"
	HealthAPIPath   = "/health"
	AuthAPIPath     = "/auth"
	RegisterAPIPath = "/register"
)

const (
	HeaderRequestID string = "X-Request-ID"
)
